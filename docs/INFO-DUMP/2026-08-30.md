TODO
### Combined MISO and EINK_DC
- Using MUTEXES to prevent access to device operation while the SPI or OUTPUT mode is being used.
- Ensure sequential operation such that when work is needed to be done on either device it does
not depend on other device waiting on some operation.

### Implement esp_partition_register_external
- Allow using littlefs by making and registering a partition with label.
- Use custom_flash_driver example

### Two partition
1. Partition for simple Key Value Store (Settings and simple "Hardcoded Values"
2. LittleFS partition for storing the journal and the actual files.

### LittleFS Partition
- Append base journal which compacts 30 Mins if not connected to bluetooth and 1 minute if connected to bluetooth device
- Three folders.
1. Original Base files
2. Files after compaction (with Journal to verify)
3. File while in process of Syncing to prevent error while buffer.
