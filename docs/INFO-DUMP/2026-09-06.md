### Issues with esp-idf
- Need to set true of every company flash chip in Auto-Detect flash chip
even if only using winbond in this project case. (Might file a bug if it replicate)
- No simply way to default store external partition in fact might have to make
a script for it. (Not doing it yet).

### Plan Change
1. Only one partition which is LittleFS in external.
2. Adding Capacitor to circuit. (Done in real life ToDo in schematic.
3. NVS partition in internal storage. (Easy way to have default store)
4. Figuring out a way to have sdkconfig value be changed from default in git clone.

